package com.example.runningapp.other

// Note that an enum class is a class that only contains constants and we can simply enumerate the constants
enum class SortType {
    DATE, RUNNING_TIME,AVG_SPEED,DISTANCE,CALORIES_BURNED
}